

The twist code is written in normal python and source file is
twist.py

The write code is written in cython, and source files are
writhe_cython.pyx
setup.py


To compile, I ran the following command
python setup.py build_ext --inplace